import { LIGHT_GREY } from 'globals/AppStyles';
import { StyleSheet } from 'react-native';

const SearchStyles = StyleSheet.create({
    list_container: { backgroundColor: LIGHT_GREY },
});

export default SearchStyles;
